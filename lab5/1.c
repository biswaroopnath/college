#include<stdio.h>
#include<string.h>
#include<conio.h>
#include<stdlib.h>
int binarySearch(int arr[],int n, int key)
{
    int high,low,mid;
     low=0;
    high= n-1;
    while(high>=low)
    {
        if (arr[mid]==key)
    }
}

int main()
{

return 0;
}